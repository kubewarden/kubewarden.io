# Configuration file to customize the build process of the website when
# previewing it on Netlify.
[build]
publish = "public"
command = 'hugo --config config.toml,config-netlify.toml --baseURL "${DEPLOY_PRIME_URL:-${URL:-https://www.kubewarden.io/}}"'

[build.environment]
HUGO_VERSION = "0.166.0"
